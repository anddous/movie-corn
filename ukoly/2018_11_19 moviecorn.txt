#1 => zprovoznit debug v Pythonu
#2 => zprovoznit filtr na rating
#3 => pridat tconst parametr do tlacitka vysledku hledani (zjistit z databaze)
#4 => po kliku na detail 
	a) => zjistit z databaze podle parametru tconst informace, ktere mame k dispozici v nasi DB
		{rozsireni DatabaseServise tridy + vytvoreni procedury v SQL Serveru}
	b) => doplnit udaje z OMBD API podle nazvu filmu a roku
		{vytvorit novou tridu pro pousteni API, ktera vrati potrebne informace}
		wishlist => po jednom nacteni ulozit k nam do DB (abychom opakovane nepousteli pozadavek na API)
	c) => vykreslit vse v detail strance


return template (cela stranka)
http://flask.pocoo.org/docs/0.12/quickstart/#rendering-templates

Partial view: (cast stranky)
https://stackoverflow.com/questions/8971887/flask-partial-view-like-mvc-3