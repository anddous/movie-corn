$.ajax({
                url: "@Url.Action("GetModal","General")",
                type: 'GET',
                cache: false,
                data: {projectId: id},
            }).done(function(result){
                $('#modalPartialView').html(result)
                $('#modalPartialView').modal('show')
            }).fail(function (){
                alert('Ajax error happened.')
            });

$('#modalPartialView').modal('show') tam být nemusí

modalPartialView je identifikátor DIV tagu v HTML stránce, který budete chtít nahradit za výsledek hledání, takže si to můžete přejmenovat

data: {projectId: id}, je JSON s parametrama, který pošlete na FLASK => tj Values z vašich select listů
url: "@Url.Action("GetModal","General")", přepíšete na url FLASK operace
url: "\api\Get...."
a pres jquery musite nejak ty values z tech ciselniku take ziskat
bud $('#year).val();
nebo pres change event
$('#year').change(function() {
  alert($(this).val());
});


-----------------


mysql = MySQL()
mysql.init_app(app)
cursor = mysql.get_db().cursor()

@movieCorn.route("/api/GetSearchResult"..parametry)
def GetSearchResult():
	data = database.GetData();
	return Json({}) 

@movieCorn.teardown_appcontext
def close_db(error):
    if hasattr(g, 'sqlite_db'):
        # Bezpečně ukončí spojení s naší databází
        g.sqlite_db.close()
'''

----- vysvětlení tříd


class => predpis, sablona
object => materializovana trida / inicializovana trida / instance tridy

class DatabaseService
{
	__init__(znacka)
	{
znacka = znacka
	}


	string znacka
	pocet_kol
	RozjedSe()
}

bmw => new auto("BMW")
bmw.RozjedSe()
bmw.znacka ==> BMW

auto.znacka = "Skoda"

int foo
foo = 4